const loginsButton = document.querySelector('.login-btn');
const backButton = document.querySelector('.login-btn');



backButton.addEventListener('click', () => {
    window.location.href = "index.html"
});

loginButton.addEventListener ('click', () => {
     window.location.href = "index.html"
})